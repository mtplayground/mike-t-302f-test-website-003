import './App.css'

function App() {
  return (
    <main className="app-shell">
      <section className="intro-panel" aria-labelledby="app-title">
        <p className="eyebrow">Vite + React + TypeScript</p>
        <h1 id="app-title">mike-t-302f-test-website-003</h1>
        <p className="lede">
          A production-ready frontend scaffold with typed React, Vite build
          tooling, linting, and formatting configured.
        </p>
      </section>
    </main>
  )
}

export default App
